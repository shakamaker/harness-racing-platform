# parser

HTML → dataclass → ORM upsert pipeline (CLAUDE.md §4.2). **Owner: Agent 2.**

Sprint 0 stub:
- `to_ss_ms()` utility with full test coverage (CLAUDE.md §4.2.3)
- Pydantic shells for Meeting / Race / Runner
- `parse_results_html()` skeleton (no DB write yet)
